{
  "name": "@ai-commerce/database",
  "version": "0.1.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "migrate": "node dist/migrate.js",
    "seed": "node dist/seed.js",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@ai-commerce/types": "*",
    "postgres": "^3.4.3",
    "drizzle-orm": "^0.29.0"
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "@types/node": "^20.10.0",
    "drizzle-kit": "^0.20.0"
  }
}
