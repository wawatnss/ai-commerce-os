
# Build context for this Dockerfile MUST be the monorepo root so that npm
# workspaces (apps/*, packages/*) and Turbo can resolve internal
# dependencies such as @ai-commerce/ui, @ai-commerce/types and
# @ai-commerce/shared.
#
#   docker build -f docker/Dockerfile.web -t ai-commerce-web .

FROM node:18-alpine AS base

# ---- Build the whole workspace, then let Turbo build only what @ai-commerce/web needs ----
FROM base AS builder
WORKDIR /repo

COPY . .
RUN npm install
RUN npx turbo run build --filter=@ai-commerce/web...

# ---- Production runtime image ----
FROM base AS runner
WORKDIR /app

ENV NODE_ENV=production
ENV PORT=3000

RUN addgroup --system --gid 1001 nodejs && \
    adduser --system --uid 1001 nextjs

# Next.js "standalone" output (see apps/web/next.config.js) traces only the
# dependencies actually used at runtime, keeping the final image small.
COPY --from=builder /repo/apps/web/public ./apps/web/public
COPY --from=builder --chown=nextjs:nodejs /repo/apps/web/.next/standalone ./
COPY --from=builder --chown=nextjs:nodejs /repo/apps/web/.next/static ./apps/web/.next/static

USER nextjs

EXPOSE 3000

CMD ["node", "apps/web/server.js"]
